#ifndef PLATO_MAIN_LIBRARY_SPLASH
#define PLATO_MAIN_LIBRARY_SPLASH

#include <ostream>

namespace plato::main::library
{
/// @brief Writes the Plato spash screen to @a aStream
void print_splash_screen(std::ostream& aStream);
}  // namespace plato::main::library

#endif
