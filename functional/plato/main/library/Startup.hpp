#ifndef PLATO_MAIN_LIBRARY_STARTUP
#define PLATO_MAIN_LIBRARY_STARTUP

namespace plato::main::library
{
/// @brief Called at the start of the program, this loads any plugins or performs other initialization required by the
/// libraries.
void startup();
}  // namespace plato::main::library

#endif
